$frontend-design $theme-factory

If this repository is not yet a runnable Next.js App Router + TypeScript + Tailwind project, scaffold the minimal working app first, then build the landing page.
Build the Safo landing page in this repository.

Read and follow:
- AGENTS.md
- docs/wireframe.txt
- content/content.yaml

Requirements:
- Build a single-page landing page only
- Use `content/content.yaml` as the single source of truth for copy and structured content
- Follow the exact section order from `docs/wireframe.txt`
- Use Next.js App Router, TypeScript, and Tailwind CSS
- Keep the design mobile-first, premium medical aesthetics, clean, restrained, and fast to scan
- Keep one primary conversion goal: the embedded contact form
- Implement filter chips for treatment goals
- Implement device card CTA behavior that prefills `selected_device`
- Add a mobile sticky CTA
- Add metadata, FAQ schema, and `noindex`
- Do not add extra sections or filler content
- Do not rewrite the copy beyond minor UI fit adjustments
- Do not use stock-doctor or spa-style visuals

Stop when the page builds cleanly.
Then show:
1. changed files
2. assumptions
3. anything still stubbed